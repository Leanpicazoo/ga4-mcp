{
  "manifest_version": "0.3",
  "name": "ga4-mcp",
  "display_name": "Google Analytics 4 (Official)",
  "version": "1.0.0",
  "description": "Connect your Google Analytics 4 data to Claude. Packages the official Google Analytics MCP server (read-only). REQUIRES Python 3.10+ and pipx installed on your machine — see the repo README for setup.",
  "long_description": "This is a Claude Desktop extension that runs the official Google Analytics MCP server (github.com/googleanalytics/google-analytics-mcp, PyPI package 'analytics-mcp'), maintained by the Google Analytics team. It gives Claude read-only access to your GA4 accounts and properties via the Analytics Admin & Data APIs: account/property discovery, core reports, funnel reports, realtime reports, and custom dimensions/metrics.\n\nPREREQUISITES (the extension cannot bundle these): (1) Python 3.10 or newer, (2) pipx on your PATH, (3) the Google Analytics Admin API and Data API enabled in a Google Cloud project, and (4) credentials with the analytics.readonly scope — either a Service Account JSON key (simplest) or user credentials from `gcloud auth application-default login`. Fill in the path to that credentials file and your Google Cloud Project ID below. Full step-by-step setup is in the README: https://github.com/Leanpicazoo/ga4-mcp",
  "author": {
    "name": "Leanpicazoo",
    "url": "https://github.com/Leanpicazoo"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Leanpicazoo/ga4-mcp"
  },
  "homepage": "https://github.com/Leanpicazoo/ga4-mcp",
  "documentation": "https://github.com/Leanpicazoo/ga4-mcp#readme",
  "icon": "icon.png",
  "server": {
    "type": "python",
    "entry_point": "server/main.py",
    "mcp_config": {
      "command": "pipx",
      "args": ["run", "analytics-mcp"],
      "env": {
        "GOOGLE_APPLICATION_CREDENTIALS": "${user_config.google_credentials_file}",
        "GOOGLE_PROJECT_ID": "${user_config.google_project_id}"
      }
    }
  },
  "tools": [
    { "name": "get_account_summaries", "description": "List the user's Google Analytics accounts and properties." },
    { "name": "get_property_details", "description": "Return details about a specific GA4 property." },
    { "name": "list_google_ads_links", "description": "List Google Ads account links for a property." },
    { "name": "run_report", "description": "Run a core GA4 report (Data API): metrics, dimensions, date ranges, filters." },
    { "name": "run_funnel_report", "description": "Run a GA4 funnel report (Data API)." },
    { "name": "get_custom_dimensions_and_metrics", "description": "List the custom dimensions and metrics configured for a property." },
    { "name": "run_realtime_report", "description": "Run a GA4 realtime report (last 30 minutes)." }
  ],
  "keywords": [
    "google-analytics",
    "ga4",
    "analytics",
    "mcp",
    "model-context-protocol",
    "claude",
    "google"
  ],
  "license": "MIT",
  "user_config": {
    "google_credentials_file": {
      "type": "file",
      "title": "Credentials file (Service Account JSON or ADC)",
      "description": "Path to your Google credentials JSON with the analytics.readonly scope. Easiest: a Service Account key file. Alternative: the ADC file created by `gcloud auth application-default login` (printed as 'Credentials saved to file: ...'). The associated identity must have access to your GA4 property.",
      "required": true
    },
    "google_project_id": {
      "type": "string",
      "title": "Google Cloud Project ID",
      "description": "The Project ID where you enabled the Google Analytics Admin API and Data API (e.g. my-project-123456).",
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=0.11.0",
    "platforms": ["darwin", "win32", "linux"],
    "runtimes": {
      "python": ">=3.10.0"
    }
  }
}
